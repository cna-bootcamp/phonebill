apiVersion: v1
kind: Service
metadata:
  name: product-service
  namespace: phonebill
  labels:
    app: product-service
    app.kubernetes.io/part-of: phonebill
spec:
  type: ClusterIP
  selector:
    app: product-service
  ports:
  - name: http
    port: 8083
    targetPort: 8083
    protocol: TCP
