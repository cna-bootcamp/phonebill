apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: phonebill
  labels:
    app: user-service
    app.kubernetes.io/part-of: phonebill
spec:
  type: ClusterIP
  selector:
    app: user-service
  ports:
  - name: http
    port: 8081
    targetPort: 8081
    protocol: TCP
